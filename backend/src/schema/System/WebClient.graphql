type WebClient {
    id: ID!
    name: String!
    password: String!
    webTools: [WebTool]
    webUsers: [WebUser]
}
input WebClientFilter {
    id: ID
}
input WebClientForm {
    name: String!
    password: String
    webTools: [WebToolFilter]
}