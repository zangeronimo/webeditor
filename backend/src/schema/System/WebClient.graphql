type WebClient {
    id: ID!
    name: String!
    password: String!
    webTools: [WebTool]
}
input WebClientFilter {
    id: ID
}
input WebClientForm {
    name: String!
    password: String
    webTools: [WebToolFilter]
}