type Query {
    sayhello: String!

    users: [User!]!
    user(filter: UserFilter!): User

    webClients: [WebClient!]!
    webClient(filter: WebClientFilter!): WebClient

    webTools: [WebTool!]!
    webTool(filter: WebToolFilter!): WebTool

    webRules: [WebRule!]!
    webRule(filter: WebRuleFilter!): WebRule
}