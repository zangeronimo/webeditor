type Query {
    sayhello: String!

    users: [User!]!
    user(filter: UserFilter!): User

    webClients: [Client!]!
    webClient(filter: ClientFilter!): Client

    webTools: [WebTool!]!
    webTool(filer: WebToolFilter!): WebTool
}