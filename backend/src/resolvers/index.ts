import Query from './Query';
import Mutation from './Mutation';
const WebClient = require('./Type/WebClient');
const WebTool = require('./Type/WebTool');

export = {
    Query,
    Mutation,
    WebClient,
    WebTool,
}